# 变更记录

版本号是本 Skill 自己的迭代号，不对应即梦或 Seedance 的版本。未标日期的历史版本为 2026-09-21。

## v32 规则去粉色大象（2026-09-26）

来源：对全部规则文件做的一次"粉色大象陷阱"体检（废弃历史叙事、纯负向约束、机械脚手架）。只改措辞与位置，规则含义、脚本、测试都没动；回归 594/18/53/71 不变。

- **SKILL.md**：「禁止事项」改为「交付物只含」（正向四条）；第 5 条不再逐个点名旧格式特征；第 7 条只留"每镜、每稿自足"并指向 writing-rules 第 60、61 条；第 2 条删掉加粗的"不设概述段…"（前后的正向句已说清）。
- **writing-rules.md**：第 20、29 条与成文主规则 2、第 10 条里的失败史叙事（L091 / L093 / L099 / L100 / L101 / L102 的翻车画面）只留结论与编号，叙事在 lessons；第 12 条删第二次禁用词枚举；成文主规则 4 的发力枚举只留在第 20 条。
- **revise-rules.md**：第 3 节的五段 / 六段旧壳细则与迁移步骤搬到文末第 13 节（父稿是旧壳时才读）；第 8 节不再复述 writing-rules 第 60、61 条的禁用短语与例句。
- **seedance-format.md**：第 6 节不再复述"那次崩成的…不进提示词"，改为指向 writing-rules 第 61、62 条。

## v31 经验库出口与两项串行检查（2026-09-25）

来源：9/20–9/25 五个镜头约 100 条成片的复盘。结论：有效写法就那么几条，大部分时间耗在改稿改坏原来好的东西、同一失败换措辞反复重试；经验库五天加了 59 条、只进不出，4 个分类超 15 条上限。用户原则：不降低提示词质量、skill 越来越聪明、提高效率；经验里有抽卡也有写得不到位，要甄别。

- **`references/lessons/archive.md`**：经验库的出口。已升级为规则、已撤回推荐、已并入三种条目整行搬过去，主库只留活条目；编号不腾不重用。首批搬 17 条（6 升级 + 3 撤回 + 5 合并 + 3 只为支撑撤回的单次观察），主库 123 → 107 条，摄影 / 多人与站位 / 特效与形态回到 15 条以内（景别与画外仍 16，其中 L101、L103 是单次观察，下月按保质期处理）。
- **合并保留哪条**：改为保留证据更强、结论更完整的那条，不看编号先后（L066→L067、L057→L076、L063→L062、L117→L083、L116→L115）；被并条目的要点括进保留条结论末尾。
- **五个脚本认识 archive**：`log_lesson.py` 取号看两文件合集；`lint_lessons.py` 按合集查编号连续、查两边重复；`lint_cases.py` 的 `→ L0xx` 指到 archive 也成立；`merge_lessons.py` 不把目标已归档的编号当新增加回；`review_lessons.py` 第五节改为"主库里带标记没搬走的 + archive 现状"，新增第六节"单次观察满 30 天没有第二个来源"。
- **三条防再胖**（README 条目格式 + 整理经验节）：① 主库 `<!-- 整理于 <日期> L0xx -->` 标记，`log_lesson.py` 在标记后满 10 条提醒整理；② 来源只有一条成片的结论开头写"单次观察"，同一稿再跑结果不同记作生成波动不立条目，单次观察 30 天没第二来源列为归档候选；③ 写法 A / B 各 80 字内、整句放案例库，超长只提醒。
- **`check_prompt.py` 两项提醒**（灯笼怪复盘的两个盲区）：没有终点的程度词（同一句有"越来越 / 越变越 / 不时 / 不断"又没有"涨到 / 退到…就停 / 占满 / 出画"这类终点，L098）；串行运镜句数（镜头 / 相机为主语的运动句 × 0.5 秒超过镜长一半，一镜到底里相机动作是串行的，数量每镜进 checked）。都是提醒不是错误，词表在脚本头部可调；四个新用例。
- **经验 L124**（摄影/镜头穿过光柱壁被切成两镜，已试，三版三切）；L068 现象列补"垫图僵硬"的用户原话（僵 = 构图、姿势、材质三种侵入叠加）。
- **diagnose.md**：第 5 节加"先查稿、再谈模型"（几何矛盾、围栏句、无终点程度词三样先查，灯笼怪误归因容量一例）；第 8 节加灯笼怪转身连改五版没换方法的反例。
- 没动：SKILL.md、writing-rules.md（v25 定案，用户没点名）；案例库。

## v30 借鉴花叔 seedance-skill（2026-09-24）

来源：对照 alchaincyf/seedance-skill（f14d8fa，2026-08-25），用户选定借鉴 7 条，全部标外部来源、未在本项目成片上试。它的提示词写法（`@图片N`、正文写时长画幅、纯时间戳分段）与本 Skill 既定规则冲突，不采用；白模引擎未安装。

- **camera.md 第 3 节**：焦段 × 距离 → 景别换算表（24/35/50/85mm × 全景到特写 + 人占画高 1/5），按本 Skill 景别口径与 16:9 重算（原表是 3:2、景别口径不同），只用于内部设计让多镜机位与巨物透视自洽，不照抄进正文。
- **camera.md 第 7 节**：相邻两镜拍同一主体，景别至少跳一档或机位换 30 度以上（通用剪辑规则）。
- **staging.md 第 5 节 + 第 9 节检查**：两人以上同框先按机位查画面上会不会叠成一个（世界里远 ≠ 画面上分得开），每人给画面位置，环绕换侧每拍重写。
- **staging.md 第 6 节**：一镜到底还是切的判断——爽点在空间连续就一镜到底，在关系对撞就切。
- **seedance-operations.md 第 5 节**：白模三个副作用（颜色渗进成片、反常识姿态钉不住、主体太小不动被丢掉）；两条白模由提示词调度多机位的候选写法（示例已过 check_prompt）。
- **经验库 L121（未试）**：主体占画高不到一成且位移不到半个身高会被丢掉，和 L090 同类。
- 检查器未改；回归 580/18/53/71 不变，经验库 121 条。

## v29 猿三法相复盘落库（2026-09-24）

来源：临渊行 PV「猿三宣判 + 法相天地」四镜稿 11 版迭代（260924-1 至 -11，其中 -1、-3、-7 未生成），两条用户通过的成片：视频节点 5.mp4（260924-2）与视频节点 5 - 副本 (6).mp4（260924-11，最终版），用户原话"这两条都可以"。用户自己总结两条总原则：①每一镜写上构图、景别、运镜、视觉焦点、光、景深；②世界是流动的，看到什么描述什么。另有一条用户建议：人物多时情节里写"图2的巨猿法相"这类带图号的称呼更好控。

- **经验库 L105–L120（16 条，全部已试）**：
  - L105 每镜六项，先定世界布局再按本镜机位只写看得见的（用户总结原则）；
  - L106 正面大远景加文字尺度句把巨物缩到建筑量级，近处物体出画、巨物切出画框、远近分层成立；
  - L107 参考图材质压过风格段文字，主体段写"图里是……；本片是……"，风格段再分层写；
  - L108 场景图构图靠否定句拿不走，要给斜向几何和建筑前景；
  - L109 气态角色的腿脚做前景两版长出人脚，换建筑构件当前景；
  - L110 远景里的小人物写占画面的比例和一个可见动作才出现；
  - L111 巨物头顶压云像帽子，头前无云、云放身后当亮底；
  - L112 群体写一个共同注视目标，并在每个动作句里重复；
  - L113 同一巨物换机位按新机位重算露出的面与远近；
  - L114 人物近景召唤巨手两版缩到人物尺度，降低要求改成云层撕开；
  - L115 多个巨体角色一起扑镜三版失败，换单一巨掌压镜；
  - L116 "贴着 / 蹬 / 四肢着地"让飞行角色落地跑；
  - L117 道具动作拆多段会来回变，一镜一个动作、说话时锁住；
  - L118 整幅遮挡写"铺满整个画面 + 最后半秒纯黑"；
  - L119 多角色情节里每次点名带图号（用户偏好）；
  - L120 粒子廉价感来自均匀同质的粒子，改写分层能量体（光核 / 光丝 / 带拖影火星 / 热浪折射 / 交互照明 / 暗部收住）。
  - L105、L112、L119、L120 标「已升级为规则」。L104 来源列追加 260924-11 四镜密度 147/157/76/103 字/秒；第六节待实测加 T12（480P 样片与 720P 出片的粒子质感）。
  - 引句逐条对过各版原稿：L106、L108、L111、L115 的引句按原稿改正（不同版本的句子拼在一起、场景段那半句是 -8 才改的、个别用词），L119 按抽帧写成"出场的五尊"（图5 小法相没出现）。
- **案例 M005**：图生四镜（反派召唤 + 巨物群像 + 下令 + 巨掌压镜，20 秒，8 图 + 1 音频），提示词原文是 260924-11 全文、一字不改；「我的评价」按抽帧写实际看到的，没兑现的也写上（猿三只有画面约二十分之一高、图5 小法相未出现、镜3 金眼未出现、屋脊没斜）；可复用点 18 条（16 条知识 + 2 条样板）。
- **规则本体**：
  - SKILL.md：必须守住第 1 条加"多镜稿先定世界布局，再按本镜机位推算画面里有什么"；第 8 条加"每镜首句写层次与焦点"。
  - writing-rules.md：成文主规则第 1 条加首句层次与焦点、光只写关键事件受光和本镜里光的变化；去处表加一行（层次三层、焦点、哪层虚化 → 每镜首句）；第 5 条加「矛盾核对（改句不删句）」四问；第一组第 6 条加世界布局、巨物换机位重算、群体共同注视目标。
  - seedance-format.md 第 2 节：多角色点名带图号；参考图自带的材质和构图会压过文字，给出对策。
  - craft/camera.md：第 4 节前中后景加每镜三层与焦点、巨物镜三件套；第 7 节多镜镜头一致加巨物换机位重算。
  - craft/staging.md：第 3 节 ①②③ 各加一句（全场布局、带图号、共同注视目标）；第 7 节末尾加巨物换机位；第 9 节检查加一条。
  - craft/vfx.md：第 3 节加能量体分层写法；第 8 节巨物加本项目实测；第 9 节已知坑加两条（气态角色腿脚长出人脚、"贴着 / 蹬 / 四肢着地"）。
  - review/diagnose.md：第 7 节表加四行（远景人物没出现、群体各看各的、飞行角色落地跑、结尾只占中央）；第 8 节加本项目实例。
  - review/qa-checklist.md：提示词表加 5a「世界与机位自洽」，"34 项"改为 35 项。
  - 词库：vfx-deform-trail-space-terms「巨手压天」理解度改为已试（L115），变体加"掌心朝镜头压到铺满画面（M005）"；vfx-energy-light-terms D 组加「分层能量体」一行（已试，L120）。
- 不做：check_prompt 等检查器不加新提醒；不改测试。
- 回归 580/18/53/71；经验库 120 条、案例库 5 条、词库 11 张表 989 条体检通过。

## v28 压缩审校与松紧旋钮（2026-09-24）

用户要求 skill“在有限字符里完成表达、精简而不失意”，同时担心规则写死让稿子死板。依据：Seedance 官方指南（每个句子只做一件事、避免冗余堆砌、用数字替代模糊词）、Sora 2 指南（短提示词换自由、长换控制；每镜一运镜一动作）、Runway（重复描述参考图会让画面动得少）、LLMLingua（可预测的词先删、指令部分不压）、Chain of Density（先写全再加密，过密可读性反而掉）、caveman（只压散文脚手架，数字路径代码不碰）。

- **writing-rules 成文主规则第 5 条**改为「不设字数上限，成文后过一遍压缩审校」：先写全再压；三问（删了画面变不变、和别句是不是同一件事、模型自己会不会补）；虚词清单（一路、此时、这时、继续、不断地、随之可删；逐渐、渐渐、缓缓表示变化和速度时照用、只当垫字时改成具体速度而不是删；“开始”标记起点不算虚词；时序词与速度词照用）；承重不碰清单（切线、坐标、秒数、数量、关键事件、每拍运镜与终态、素材职责、必填句、固定句、用户原话、固定外貌短语、消歧信息、看得见又带情绪的句子）；许可句“写到关键事件能看清为止，其余交给模型，没有要填满的清单”；松紧旋钮（默认 / 更放 / 更控，只改写多少不改承重；采用非默认档时写进表头待你定行）。失效腔调表下注明：压缩删的是废话不是语气。
- **SKILL.md**：轻量路径插入“压缩审校”一步；用户偏好加「松紧旋钮」；第 8 条指向。seedance-format 第 9 节“待你定”行记本次松紧档。
- **check_prompt 三条提醒（都不是错误）**：复读（情节里 6 字以上短语出现两次以上，主体段与场景段固定短语、时间点、固定句豁免，最多报 5 条）；虚词（每镜超过 3 个）；密度（每秒字数超过参考线；参考线定 90 时三个多镜成功案例 M002–M004 全部被报出来——它们在 120–200 字/秒——所以默认 `DENSITY_CHARS_PER_SEC = 200` 只抓极端过载，`--density-line` 可调，待 A/B）。复读豁免画框切线、画面坐标、运镜术语、主体段与场景段固定短语；改稿时与父稿逐字相同的镜头不报虚词和密度。新增 7 个夹具，回归 543 → 580。
- **经验库**新增「密度与节奏/密度参考线待实测」（未试）：灯笼怪 78 字/秒（用户评价自然）对 134 字/秒（细节大半未执行）；补注三个成功案例 120–200 字/秒；下次同一设计压缩版与不压版各跑一次，按结果改参考线。
- 不做：硬字数上限；电报体；一开始就写短。
- 复读提醒文案注明落幅、焦点落点、每镜自足的句子照留；第 55 条注明压缩审校属于成文、不算额外审核轮次；三问③只管镜内，主体段外形状态按第 7 条⑥。

## v27 词库扩充与「拔高」机制（2026-09-24）

用户要求“多收集不同的特效、打斗、运动等词库……把我的描述转化为更‘高级’的形容方式，帮我拔高设计的上限”，并点明要的是“细碎灵尘消散”这种细到形态的小众效果名。

- **七张新词库（732 条，全部标未试）**，条目细到具体效果名，每条五列：口语说法 / 效果名（带题材标签）→ 术语（中 / 英，没有就写 —）→ 画面上看到什么（形态、颜色、大小、密度、方向、时长、残留 + 变体）→ 可直接用的中文句（讲戏口吻，来源→路径→接触或范围→退出与残留，带画面坐标与时间）→ 理解度。
  - `vfx-dissolve-appear-terms.md` 120 条：消散（光尘、灰烬燃尽、碎裂、雾化流体、褪色影化闪断、化群）、显形聚合、转化（石化冰封结晶纸化……）、局部渐进、群体规模。
  - `vfx-energy-light-terms.md` 107 条：剑气气刃、气浪冲击波、护体屏障、体表能量、雷电、法阵符箓、光效、光的运动。
  - `vfx-particle-fluid-fire-terms.md` 118 条：发光 / 不发光粒子、气泡水珠、溅射泼洒、液体大动作、火、烟、雾与蒸汽、尘沙、天气雷雨。
  - `vfx-deform-trail-space-terms.md` 87 条：残影分身、拖尾轨迹、变形、空间、时间、视觉失真、巨物尺度。
  - `combat-terms.md` 140 条：拳肘掌、腿膝、刀、剑、枪与长兵、短兵软兵暗器、擒拿摔法、格挡化解、受击反应、群战调度、特技威亚、节奏态势（只写结果不写发力过程）。
  - `motion-terms.md` 110 条：追逐、跑酷、飞行滑翔、坠落落地、滑行、载具、群体运动、物体运动、速度节奏、水中空中。
  - `camera-combo-terms.md` 50 条：追逐跟拍、揭示、冲击、对话情绪、特效配合、转场型运镜组合（引用 M01–M10 编号，不重复）。
- **机制**：lexicon/README 改为十一表分工 + “读表先 grep 小类标题，只读命中的小类” + 「词库升级（拔高）」四条做法；SKILL.md 写新提示词路由补“按题材 grep 词库，为每个白话动作或效果挑 1 个推荐 + 1–2 个备选写进表头升级行”，新增路由「说“拔高 / 更细 / 更小众 / 还有什么写法”→ 3–5 个候选」；seedance-format 第 9 节升级行格式改为 `原 = 用户白话 → 升 = 效果名（半句可见结果）；备选：…`；directing-upgrade 加第 2.5 节「词库升级」。升级只换写法或给关键事件加一层可见结果，一拍最多一个，不加剧情人物道具。
- **质量控制**：每份新表的例句抽 30 句装成四段探针稿跑 check_prompt 零提醒，并各自把全部例句跑过一遍；新增 `scripts/lint_lexicon.py`（带“理解度”列的五列表每行五列、理解度合规、可用句不含空词 / 解释词 / 画质词 / 引用性措辞 / 发力过程），接入 `tests/run_check_tests.py`（542 → 543）；action-terms 一句“转肩换端”改成可见结果。
- **已知边界（记在 notes 待定）**：几对近重复条目分在不同表并在第 3 列写了区别（鬼火、尘环、焦散、萤火绕身、砸坑）；带血与惊悚向条目可能触发即梦审核；跨两镜的运镜组合用“镜头一 / 镜头二”占位；部分例句自带景别、运镜或绝对秒数，粘进不同镜头时要改；英文列有行内俗称不是正式术语。
- 回归 543/18/53/71；词库体检 11 张表 988 条通过。

## v26 成文主规则去处表（2026-09-23）

用户批准规则瘦身第一步（两个方案“全部执行掉”），只改 `references/writing-rules.md`：

- 成文主规则第 1 条只留骨架（三样打底、拍的定义、每镜首句），原 12 项叠加清单进表；第 3 条只留总则与改稿分支，原逐项去处进表；第 7 条一字未动。
- 新增「成文主规则去处表（第 1、3、7 条共用）」放在第 7 条之后：表头写三种通用例外一次（本镜关键事件的一部分→那一拍；用户点名→按点名写进那一拍；用户认可原句与 asks 落点→原位不动），38 行四列（内容｜写在哪｜条件 / 另外的去处｜依据），覆盖主体段、场景段、风格段、每镜首句、每拍①②③、叠加项、否定句、命令区与不写的项。
- 第 9、17、56、76、78 条里复述去处的部分压成指向；第 4 道门、第 43、60 条 v25 已是指向，不动。
- 结果：信息一条不丢、条号不变；文件 53.3 KB → 57.0 KB（+6.8%）——表格把散在各条里的去处集中列了一遍，体积没减、查找变成一处。用户选择保留整张表。seedance-format 第 1 节与 SKILL.md 第 8 条的指向改为直接指去处表。
- 回归 542/18/53/71 不变。

## v25 全库审查定案（2026-09-23）

用户要求“把 skill 整体再审查一遍，有问题就改”。五路只读审计（交叉引用、检查器对齐、工艺卡与词库、案例经验操作文档、主规则矛盾）之后，按文件分四个互不重叠的包修改，再做一次全库交叉复核。主干口径不变（四段外壳、末行固定句、否定预算 0、不写 @、主体段只写身份、每拍三样、关键事件在明处、不写总括保证句、讲戏口吻、发力只写结果、0.5 秒/拍、每镜默认可见运镜）。

**规则本体（SKILL.md、writing-rules、seedance-format、review/ 四份）**
- 消掉 20 处互相矛盾：衣物结果统一为“每拍最多一处”；“抢同一主体/朝镜头扑来”补回“另一个”；运镜可见结果由“最多带一处”改为必须一处；会变的道具状态统一为“开场现状 + 变化那一拍终态”（指向成文主规则第 7 条）；固定机位来源补“保护既有摄影”；已试例外否定句位置写死（L074 场景段、L092 主体段）；两次失败换方法只在 diagnose 第 8 节维护，范围为“同一个要求（用户要求或作者设计都算）”；轻量路径不再被引去 quality-gate 记录。
- 成文主规则第 1 条改名“每拍三样打底，叠加项按清单”：三样是下限，叠加项（对白音效、关键事件受光、情绪方向一句、对手反应、一处衣物结果等）不计入；运镜起点只在每镜首句写一次，之后每拍写往哪走、停在哪，推拉时终点切线就是可见结果，延续到下一拍只写新终点。
- 成文主规则第 2 条补两条定义：整幅遮挡指关键主体以外的东西盖住画面，主体自己冲到占满画面是关键事件本身；地面裂开、塌陷、碎裂算环境破坏照常叠上，扬尘碎屑余波算次要响应。
- 成文主规则第 3 条例外改写成一读就懂的①②③；材质响应条件改为“本镜关键事件的可见结果”；删掉“用户在意的环境破坏才叠”；冲击后才出现的静态残留写在它出现那一拍的终态；补主体段/风格段材质判据（只属于某人物/持物的表面写主体段，多对象共用的写风格段）；主体段明确例外（素材绑定、音色、全片级否定句）。
- 成文主规则第 7 条例外改成“七种不算总括句的句子”逐条列；取景例外判据改清楚（只认说画框怎么框住被拍主体的句子；被捧着的人头是道具，算总括）。
- 第 6 条：删掉“把整条轨迹都收进画面”这个“能装下”式例句；“高光点”标题改“关键事件那一拍”；一镜到底改为在镜头 1 起幅句前加“一镜到底，”。
- 第 12 条写明纯黑豁免只认“纯黑 + 时长/范围”；第 15、26、27 条例句改合规（手持用 L075 原句，跟拍写全三样）；第 20 条写明“重心移到左脚”单独不算发力过程；第 24 条“一二档跟半句、第三档五项”；第 29 条补“两个关键事件时后果停留只用在最后一个之后”；第 43 条补部分镜头才成立的天气写它开始那一镜的开场现状；第 62 条补素材句“不采用”不算否定、全片级否定只写一次的位置（身份数量类含静音写主体段末尾，环境光文字水印类写场景段末尾，只管一镜的写那一镜，操作类写命令区）、旧壳修订预算 4 条含旧固定句即自写 3 条；第 69 条补情绪方向一句不算解释词。
- SKILL.md 第 8 条压成摘要 + 指向（不再复述细则）；第 2/4 条对齐；改稿与自检路由加“涉及整拍重写、装饰层或裁定成文主规则提醒时读成文主规则一节”；L1 字段补运镜与关键事件受光；流程分级命令补 `--labels`、`--task`、`--untimed`；错引“成文主规则第 2、29 条”改正。
- seedance-format 第 1 节模板缩成短示例，规则移到代码块外并指向 writing-rules；第 6 节表格按新口径；第 9 节补 ```quote / ```draft 约定；操作命令的保持句位置与 seedance-operations 统一（身份动作机位在命令区，环境画风在场景段风格段）。
- qa-checklist 表头加“明确指能从全局段或本镜画框读出”；第 1/13a/17c/29 项改为摘要 + 指向；第 8/9/16 项不再逼作者逐拍补装饰。quality-gate 模板 complex 默认 true，新增 format、asks 字段；旧壳预算写明含固定句。

**检查器与钩子（check_prompt、verify_delivery、stop_gate）**
- 修两处错误级误报：引用性措辞按词边界（“一轮满月”“宛如上好的宣纸”“不要像木偶”不再报）；固定句上一行不再用单字“不/无/全片”判断（“不远处的灯笼……”不再报）。
- 让提醒对讲戏口吻生效：密度按 max(时序词+1, ⌈句数/2⌉) 估拍（v23 试写 24 句/6 秒 = 0.50 秒不报）；弱运镜不再要求前面有“镜头”，身体部位后的“下拉/上拉”不算运镜；关键动作词补“离开…的手/扔了出去/命中/冲镜头”，“占满画面下半/一角”不算整幅遮挡；站位保证全稿都扫；总括豁免补持物身份句、外形状态与场景段环境主语；紧跟标题的否定句能抓到，否定词补“不允许/严禁/请勿/别”；命令区带素材或操作词的句子不再报总览句；尺度名词按句取最近景别；发力词表修正；新增“能装下什么”提醒；距离链有过渡不报；主体段补情绪词与动作词；风格段补随动、表演、时序词；新稿“图片N”与同段重复绑定提醒；画质词各段都查；非整数秒提醒、超 15000 字符报错；“未识别到运动”改为只认摄影运动；无素材引用时不再提醒缺 --labels；空词跳过 --asks 关键词；没给 --task 时按命令区推断（“延续视频N”统一为必填词，编辑补“移除”）。
- verify_delivery：转发 requirements 的 asks；complex 缺省或 false 不放行；合成局部稿时传父稿外壳；读 format 字段。
- stop_gate：代跑一律按四段（旧壳新稿打回）；如实照抄“有 N 处错误”的自检行认本轮 ready=false 报告；代跑放行时附待裁定提醒条数；命令区推断复用 check_prompt（认 `a-b秒：` 标题）；裸操作命令提示套四段。
- review_lessons 认 L1xx 编号、显示最新升级标记；install.sh 与 sync.sh 只在主线 main 执行；.gitignore 加 notes/。
- 回归：run_check_tests 396 → 542（新增 36 个夹具），test_revision_checks 16 → 18，test_delivery_gate 42 → 53，test_stop_gate 61 → 71。

**工艺卡与词库（15 份）**
- 全部示范句按现行口径重写：朝向用鼻尖/下巴/后脑判据，取景写切线不写“完整可见”，发力过程改可见结果，衣物随动改每拍一处结果，定调句与保证句删掉，停镜链改为“慢步逼近 → 突然加速 → 接触强调 → 后果停留”，蓄力改“汇聚”，手持用 L075 原句，跟拍写全三样，纵深层不用“中景”，素材绑定拆到各自段落，画质词不进镜头，情绪比喻与拟人改可见句。约 70 句改后示范句装成探针稿跑 check_prompt 零提醒。
- 卡头统一“第 ⑤ 步（导演与工艺）”，camera/combat 补第 ⑥ 步，scene-mood/staging/motion-physics 必读范围收窄到题材路由；“全局设定”改“场景段 / 风格段”。
- 词库理解度列统一“档名；证据”，删“已试不稳”档；lexicon/README 只留档名，分档与写法只在 writing-rules 第 24 条；FPV 归热门、移焦归直写；用户实测标“已试（L040 用户实测，未绑定具体成片）”。

**案例、经验、操作命令与文档**
- seedance-operations：0.5 节写明检查带 `--task`/`--untimed`；延长范例镜头 1 按每拍三样重写；保持句位置、<事件> 不重复、关键帧/宫格/白模/衔接/参考各自必填词分行、“延续视频N”、编辑补“移除”、白模主体段补图职责、首尾帧/绿幕/衔接模板加装法前言；否定句位置按第 62 条。
- 案例库：旧壳六段的否定分配标旧壳并写现行做法；跟拍三件套注明现行写进镜头 1；L041、L050 按 M001/M002 成片升级为已试；L074 半句可省、L081 否定不照抄、M003 标 2.0 兑现；official-cases 去掉自写的“写进禁止项”“@参考视频”，站位保证与“承接上一阶段”加注。
- 经验库：L091、L093 结论列最前加【已撤回推荐】；L102 补 v24 注记；L001/L002/L004/L007/L008/L013/L016/L027/L037 加“已被现行规则取代”注记；来源补“模型版本未知”；T06 加注、T07 作废；lessons/README“已试”放宽为含用户本人实测（与第 29 行及 L064 实践一致），“没试过/未试/待验”同义；log_lesson.py 文档串同步。
- README/SETUP/hooks README/openai.yaml/CHANGELOG 错引与过时描述修正；钩子命令统一 `python3 -X utf8 $HOME/.claude/skills/aigc-video/hooks/stop_gate.py`、timeout 30。

**本轮替用户定的口径（可撤回）**：整幅遮挡定义；环境破坏与次要响应分界；残留去处；材质主体段/风格段判据；全片级否定只写一次的位置；一镜到底写法；两个关键事件的后果停留；部分镜头天气去处；会变的身体外形去处；L1 字段补运镜与受光；两次失败范围；用户实测算已试；L041/L050 升级为已试；SKILL.md 第 8 条只留摘要；seedance-format 模板缩短。

**未做、待用户定**：writing-rules 内部纯重复段是否删成指向（本轮只压成指向）；成文主规则 1/3/7 改“内容 → 去处”表；整体规则瘦身；静音句仍在主体段。
- 交叉复核（Opus）再修 30 余处残留：否定句位置旧文案（检查器、案例、操作命令、测试用例说明）、总括提醒例外补齐七种、跟拍右肩方向、词库焦点句、「已试」定义三处对齐、README 提醒清单与测试数量；追加：检查器汇总行「否定句提醒 N 句」、命令区认粗模契约句、命令区写了参考视频的运镜或白模时不报摄影运动、移焦归直写档、编辑示例的景别与运镜保持句移到命令区、SKILL.md 第 3 条写明动作或运镜参考视频在命令区、两次失败例外的已试含用户实测。

## v24 按建议定案（2026-09-23）

用户 2026-09-23 对待定问题回复“按建议”，逐条落点：

- E1 左右手：全片不换手的持物写主体段，画面左右写进每镜画框。writing-rules 成文主规则第 6 条“位置”、seedance-format 第 1 节；L077 结论加注记；my-cases M002 注释同步（原文未动）。
- E2 运镜呈现关键事件，可同一瞬；短促剧震不算大运镜；去拍别处、让它变小的大运镜放前后拍。writing-rules 成文主规则第 2 条、第 26、29 条；camera.md 第 4、5 节（第 46 行改写）；camera-terms 子弹时间、M03、M05、M08（恢复“命中瞬间后撤”）、M10、爆炸震动、C 节说明；combat 拆分条件；quality-gate、qa 13a；SKILL.md 第 8 条与偏好。
- E3 那一瞬只避开整幅遮挡、抢同一主体或道具的大动作、朝镜头扑来的大动作；对手反应、破坏、流液照常叠。成文主规则第 2 条维护，第 19、53 条、SKILL.md 第 8 条指向。
- E4 每拍最多一处衣物、头发或持物的可见结果，作为主事结果，不写随动过程、不写主体段；人物或背景角色身上会动的材质响应不在放宽范围内，仍只在是本拍关键动作的可见结果时写。成文主规则第 3 条维护；成文主规则第 1 条、第 15、17、56、76 条，seedance-format 第 1、3 节，SKILL.md 第 2、8 条，qa 17b，scene-mood，my-cases M003 注释。check_prompt 文案归脚本侧。
- E5 revise-rules 第 11 节：动作设计变了整拍重写（保护项仍保留），新增第 5 类去向与丢句裁定，第 10 节摘要写明整拍重写的拍。
- E6 镜头标题秒数：不改。
- E7 停镜范围：不改，核对 writing-rules 第 29 条、camera.md、camera-terms、quality-gate 一致。
- E8 密度：连续动作每拍 0.5 秒可行，1 秒超过 2 个互不相连的动作才算密，正好 0.5 秒不提醒。writing-rules 第 72 条、qa 第 4、6 项、quality-gate 第 6 步、README；L064 结论加注记。
- E9 有意纯黑写明时长或范围、按哪处光曝光可用（L073、L074）。writing-rules 第 12 条、seedance-format 第 6 节表格。
- E10 已试否定例外 L073、L074、L080、L092：writing-rules 第 62 条列原句，seedance-format 第 6 节与 qa 第 21 项指向。
- E11 镜头运动不用情绪解释；表演可在可见表现后带一句情绪方向。writing-rules 总原则、效果词表后一段、第 47、50 条；performance、scene-mood 第 1 节。
- E12 “高光点”理由改为“可能被读成亮部高光（推测，未试）”：成文主规则第 6 条、camera-terms。
- E13 景别中文口径（中景膝盖以上、中近景腰部以上、近景约胸部以上、特写面部或肩部以上）：camera-terms B 表拆出中近景并补切线，camera.md 第 1 节指向；writing-rules 例句核对一致。
- E14 静音：不改。
- E15 writing-rules 第 5 条补：主体、场景、风格段的表面材质词不受尺度规则约束。参考视频位置、同稿两跑：不改。
- check_prompt 与回归：密度改成平均不到 0.5 秒一拍才报（E8）；有意纯黑的豁免（E9），镜头标题时码和“第N秒”不算时长；尺度名词只扫镜头正文的注释（E15）；主体段随动提醒改说镜内每拍一处（E4）。新增 7 个夹具、修改 1 个（dense_two_per_second 改成正好 0.5 秒一拍不提醒），回归 356 → 379；M001 不再报纯黑，M003 不再报密度。
- 审查后同日修正：已试否定例外每条单独成句写，`--negative-exception` 点名时对全稿逐句整句比对，句中否定也能点名（checked 记“已试否定例外 N 句已点名”），片段仍报错（writing-rules 第 62 条、seedance-format 第 6 节）；“全程用右手握棍”这类哪只手持物的句子不算总括保证句（E1）；密度粗估里“先”“同时”不另算一拍；纯黑豁免要同句写明时长或范围，只写曝光不算，“一成不变”“大半圈”不算范围，“不采用图中的纯黑背景”这类排除分句不扫（第 12 条改成“能写就再写按哪处光曝光”）；--baseline 说明补上第 11 节第 5 类（整拍重写）。景别“特写怼眼睛”“眼睛特写”改成大特写（camera.md、camera-terms、directing-upgrade）；效果表“轻盈”改成一处结果；scene-mood 五维与灵魂层例句改成可见句后带一句情绪方向；SKILL.md 第 8 条补上去拍别处的大运镜放前后拍，流程分级那句改成指向；quality-gate 第 5 步不再用“只有”限死镜内内容；E4 那句在 SKILL.md 第 2 条、seedance-format 第 1、3 节、scene-mood、my-cases M003 注释与检查器提醒里压成指向。新增 6 个夹具，回归 379 → 396；M002、M003 按六段跑不再报纯黑。v24 两轮合计新增 13 个、修改 1 个夹具；tests/cases.md 的 H9、I1 同步。
- 没改的：四段外壳、不写总览句、末行固定句、素材写法与绑定、默认可见运镜、否定句默认不用、认可内容与 asks 保护、轻量默认、两次失败换方法、同音替换、成文主规则第 1–7 条主干。

## v23 不写总括保证句，主体段只写身份（2026-09-23）

- 用户原则（原话要点）：情节里没看出来灯笼怪持续在镜头中，加一个总览写“持续在镜头中”也没用，要么穿帮，要么互相打架；总览不是重点，重点是精进镜头中的画面描述。用户另指出试写稿“主体的时候把一部分情节里的东西也写进去了”。
- 取消连续镜头锁（L087）：v22 写进 seedance-format 主体段清单、writing-rules 成文主规则第 6 条的“全片是一个不断开的连续镜头，某某在任何时刻都至少有一部分留在画面里”撤回。全片一镜到底时，连续靠每一拍写清画里有谁，设计里不出现只剩空景的一拍（L087 真正的教训：空画面处模型会切镜）；“一镜到底”可作风格段的镜头性格或镜头1第一句，不附带“任何时刻……留在画面里”。writing-rules 成文主规则第 6 条维护，camera.md 第 7 节、genre-recipes 第 10 节、qa 第 3 项、diagnose 第 7 节新行只留指向；L087 结论列末尾加了用户决定的注记。
- 主体段只写是谁、长什么样、有几个：素材绑定（图N 用于什么、不采用什么）、外形与参考图看不出的状态（湿透、伤势）、固定短外貌短语、数量锁、音色绑定、背景角色的外形身份。撤回 v21 写进主体段模板的三处（人物与持物的共享随动、角色类背景群体的身份与共同路径、连续镜头锁）。动作、表演动作（“上下牙不停地磕”）、衣物头发持物的随动、背景角色的路线与活动、镜头与调度、会在镜内改变的道具状态（拿着、提着、被扔出）写进情节里它发生或第一次出现的那一拍，只写一次；衣物随动只在是关键动作的可见结果时写，否则交给模型。段落归属在 seedance-format 第 1、3、6 节；装饰层取舍在 writing-rules 成文主规则第 3 条，改名“装饰层不逐拍”。SKILL.md 必须守住第 2 条加一句。
- 新增 writing-rules 成文主规则第 7 条“总括保证句不代替镜内描述”：镜内没写出来的，总括句补不上，只会穿帮或打架；镜内已经写出来的，不用再总括。例外只有身份与数量事实（“全片只有一颗人头”，官方 A3 同样写法）、唯一光源这类全局光源事实、风格段的画风与镜头性格（“全程手持”）。SKILL.md 必须守住第 8 条只放一句指向。
- 场景段仍写全片贯穿的环境状态与天气反应（雨落在哪、风吹动什么）；环境里的角色活动（背景人群、冤魂飞行）进情节里它第一次出现的那一拍。风格段规则不变。
- check_prompt 新增两类提醒，只提醒不拦截，词表 SUBJECT_*_RE / GUARANTEE_*_RE 在脚本头部常量里、可调：主体段里的动作、随动、会变的持物状态与调度词（只扫四段稿；素材绑定句里的“用于、采用、定义”和静态外形词不算，“倒提”这类会变的持物状态照报）；总括保证句（强档“任何时刻”“至少有一部分”“始终留在画面”“不断开的连续镜头”全稿各外壳都扫，宽档“始终、全程……”只扫四段稿主体段和镜内以“整段里 / 全片”开头的句子，数量锁、唯一光源、身份锁、镜头性格不算）。改稿时父稿原句照报并标“父稿原有”，--partial 不报父稿原句。新增 7 个夹具（含用户指出问题的两份灯笼怪试写稿），回归 228 → 272；M001–M004 原文不误报（M001–M003 是六段旧稿，主体段提醒不扫；M004 按四段扫也不报）。
- 同步：qa 第 1、3、13a、17b、17c、29 项，quality-gate 第 5 步，revise-rules 第 11 节第 4 类（父稿主体段里的动作与总括句只在获准改写主体段或全稿时移走，删之前先确认镜内已写出），staging、motion-physics、scene-mood、directing-upgrade 工艺卡，两份案例库的规则性文字与可复用点注释（历史原文不动），operations 白模示例与超长视频装法，tests/cases.md A6、I1，新增 I3。
- 审查后同日修正：成文主规则第 7 条先划出管辖范围——场景段写的全片贯穿环境状态与天气反应（“雨丝一直斜着落下”“风一直吹动破幡”）是环境，不归本条；镜内取景约束只有点名了具体部位或画框切线（“头部和双肩始终留在画内”“画框下缘切在膝盖”“它在画面里的大小和位置始终不变”）才不算总括，只说“某某始终在画面里 / 不离开画面”的写在哪都算，“全镜 / 本镜”不再豁免；道具归属统一为：全片不变的（固定道具属于谁、一直拿在谁手里、全片不换手）是身份层面的事实，可以写主体段，会在镜内改变的（拿起、提着之后被扔出、被接住、换手）写在那一镜的开场现状和变化那一拍的终态（seedance-format 第 1、6 节、官方案例 B5 注释、成文主规则第 7 条、检查器豁免同步）；背景群体在每一镜第一次出现时写一次路线与活动，多镜稿在它再出现的那一镜再写一次（每镜自足，第 60 条），同一镜后面的拍只在它参与画面事件时再写（成文主规则第 1、3、7 条）；人物或背景角色身上会动的材质响应（白烟被风剥离、肩甲高光随转动移动）和衣物随动一样，只在是本拍关键动作的可见结果时写进那一拍，否则交给模型（成文主规则第 3 条、seedance-format 第 3 节）；主体段模板去掉“情绪底色”，情绪落成镜内可见表演（seedance-format 第 1、3 节）；单图绑定里的群像位置（L086）说明是素材职责、不算站位调度；第 24 条与风格段说明“一镜到底”在风格段只作镜头性格、不写路径，围绕谁、从哪到哪写在镜头1的拍里；第 13 条和 camera.md 景别例的正例去掉非关键的风吹头发；directing-upgrade 的次要粒子与地面响应会动的写进它发生的那一拍，静态的写场景段；成文主规则内部引用统一写成“成文主规则第 N 条”，L100 例子放回成文主规则第 2 条下面；散在各处的衣物随动与背景路线整句复述压成一句指向，只在成文主规则第 3 条和 seedance-format 第 1 节写全。L087 结论列最前面加“【2026-09-23 已撤回推荐：不再写全片连续镜头锁，见末尾注记】”，lessons/README 补“撤回推荐”标注格式，review_lessons.py 第五节列出已撤回推荐的条目。check_prompt：强档补上“持续出现 / 持续出来在镜头中”“始终在画面里”“全程都在画面里”“从头到尾没有离开画面”“都不出画”“从不离开画面”；新增场景段角色活动提醒（冤魂、喽啰、人群、路人、行人、士兵、群像……后面跟着飞、走、跑、掠、穿过、经过、盘旋……，只扫四段稿，天气与环境反应、鸟群、落叶不报）；主体段词表“晃眼”“掠食”不报，“手持”后面四个字以内有道具名词才算持物（手持长剑报，手持近距离镜头不报），持物提醒说明可裁定、写明“不换手”的不报，“伤口持续渗血”不算总括；镜内总览句延到句号、“；”接着的分句一并列出；修好 input_sha256（循环变量覆盖了原稿，不同的稿算出同一个值，v16 起就有）。新增 4 个夹具，回归 272 → 309；M001–M004 原文仍不报（测试注释写明 M002、M003 按四段扫会报主体段动作，所以主体段与场景段提醒只对四段新稿启用）。
- 第二次复查修正：成文主规则第 7 条写明取景例外只认镜头正文里的普通句，且点名的部位是这句的主语——写在主体段、场景段、风格段或镜内“整段里 / 全片”总览句里的“头部和双肩始终留在画内”、站位保证“两人的左右位置全程不变”照算总括；参考图看不出的外形状态（一直湿透、伤口持续渗血）不是保证。成文主规则第 3 条（与成文主规则第 1 条的例外清单）补上导演升级选定的次要粒子与地面响应的去处（会动的写进它发生的那一拍一句，静态的写场景段，没选定的交给模型），scene-mood、motion-physics 与 directing-upgrade 对齐；第 24 条补“一镜到底”作风格段镜头性格时不跟质感半句；qa 第 29 项分清靠每镜写清的（站位、空间方向、会变的道具状态）与写主体段的（跨镜数量锁、全片不换手的道具归属）；SKILL.md 第 2 条与 seedance-format 第 1 节把“随动”改成只在是关键动作的可见结果时写；quality-gate 第 5 步的“第 7 条”写全出处；README 提醒清单补上场景段与风格段的角色活动。check_prompt：取景豁免只给镜头正文普通句（主体段、场景段、风格段和总览句不豁免），部位词必须是分句主语（“捧着的人头”“腰间的人头”照报），“大小 / 位置……不变”只认“在画面里的大小和位置”；强档补“始终保留 / 处于 / 位于画面里”“一刻也没有 / 不会离开画面”；风格段也扫角色活动；总览句里的强档与宽档连同“；”后的分句并成一条，场景段、风格段同句的强档并进角色活动提醒；主体段与场景段、风格段共用一张背景活动词表（补盘旋、穿过、飘过、涌动、来来往往、赶路，主体段补发抖、颤抖，群体名词补鬼影、阴兵、亡魂、游魂；“麻绳穿过”“经过雨水冲刷”不报）；“一直湿透”“是同一颗”“同一主体”不算总括；总括提醒写明主体进每一拍的画框、背景群体每镜第一次出现时写一次；代码注释的内部编号换成文档出处。新增 6 个夹具，回归 309 → 356；M001–M004 原文仍不报（风格段角色活动一并核对）。
- 没改的：四段外壳与末行固定句、不写总览句、素材绑定、默认可见运镜、0.5 秒一拍、否定句默认不用、成功项与 asks 保护、轻量默认、两次失败换方法、同音替换、风格段只写跨镜共享质感、v21 成文主规则第 1–5 条与 v22 讲戏口吻（第 6 条）。待用户定的点（L077 左右手约定与 M002 可复用点、“关键动作的可见结果”的宽窄、全片静音句的位置）这一轮没动。

## v22 讲戏口吻（2026-09-23）

- 新增 writing-rules「成文主规则」第 6 条讲戏口吻：用导演加摄影指导在现场给组员讲戏的口吻说出每拍三样；术语直接用，后面紧跟半句看得见的结果（景别、机位的结果就是画框切线，同一拍几个术语共用一个结果）；位置用画面坐标；镜头为什么动最多一句空间或可见性理由；程度落到看得见、数得出；短句按时间往下说，镜内不加标签（台词、旁白的官方写法不算）；节奏、高光点、落幅各落到自己那一拍；口吻本身不加字。SKILL.md 第 8 条放摘要和指向；seedance-format 第 4 节、camera.md、camera-terms、lexicon/README 只留指向，细则只在成文主规则与第 24 条维护；qa 第 9、22 项是核对清单。
- 用户放弃了开头定调句：不在镜头开头总述节奏、高光点、落幅，这三样分别写进动作与运镜本身、关键事件那一拍（可带“第N秒，”）和最后一拍“落幅停在……”；仍不写总览句。
- 同一个词不再两种意思：总原则改成“口吻是导演加摄影指导在现场给组员讲戏”，“不是导演讲戏”改为“不是给演员讲情绪”；失效腔调的“讲戏腔”改名“效果腔”；“不解释意图”收窄为“不解释情绪和含义”，唯一能说的“为什么”是镜头为什么这么动；第 9 条改成“下一镜才发生的事不写进本镜”，空间理由可以点名本镜稍后的事、只点名不描述（会不会让模型提前做，未试）；第 69 条注明空间理由不算解释词。
- 术语三档都不只写术语，理解度只决定那半句写多具体（第 24 条、camera-terms、lexicon/README）；运镜起点就是上一拍结束时的画框，写过不重写（第 22 条、camera.md、camera-terms、qa 第 9 项）；第 66 条改成一句一个主体动作、镜内不加标签；camera.md 角度段改成“术语照写 + 半句结果”。
- camera-terms 补词：大远景、平拍、顶拍、荷兰角、三分线、居中、对角线、入画 / 出画、起幅 / 落幅（平拍是平视的别名，照旧直写；其余新词理解度都标未试）；“高光点”不进正文。官方案例头部说明镜内标签只看结构，SKILL.md 第 5 条把镜内标签列进不随案例带入的清单。
- check_prompt 新增两类提醒，只提醒不拦截：镜内标签（“摄影：”“动作：”“动作/表情：”“情感解析：”“第一拍，”“第4秒：”“【构图】”这类；标签词在句首并紧跟冒号才算，几个标签词用 / 、 连写也算，【标签】不需要冒号，“第N拍”在句首并紧跟冒号、逗号或句末才算；台词、锁定文字和父稿原样存在的句子不报）、镜头正文里的画质词（电影感、电影级、大片感）；词表在脚本头部常量里，可调。新增 6 个夹具（含灯笼怪 v21 试稿与讲戏口吻重写稿、官方案例式复合标签、“第N拍”不误报），回归 210 → 228；M001–M004 原文不误报。tests/cases.md 的 A5 改了合格标准，新增 I2。
- 审查后同日修正：改稿时讲戏口吻只用于获准改写的句子，认可过的措辞、逐字锁、asks 落点原句和父稿未授权部分原样保留（成文主规则第 6 条“长度”）；程度词保留范围补上运镜速度和用户原话，第 21、68 条与成文主规则第 6 条统一为“其余作者自加的删掉”；空词改成“翻译成看得见的结果，翻译不了就删”；一拍的句数改成常见长度、不是配额，换拍另起一句；台词、旁白的说话人前缀和“台词语言：”不算镜内标签；落幅按需使用，跨镜带着运动切出时不写停住；全片一镜到底的连续镜头锁（L087）写进 seedance-format 主体段清单；空间理由统一为“每镜最多一句”并补进成文主规则第 1 条的例外清单；推、拉改了景别时终点切线就是那一处可见结果，camera.md 与 camera-terms 模板同步；术语三档只在第 24 条写全，手持归直写档，“未试”只要求那半句点名对象和变化，五项公式只用于需解释档，风格段的镜头性格跟静态可见质感；第 67 条补上端机姿态类比（L075）例外；第 33 条正例改成几何判据（L059）；第 74 条、camera.md 一镜到底例、宏大例、scene-mood 退出例改成讲戏口吻（候选写法，未试）；纵深层不再写“中景”；微角度例句补可见结果（L050）；场景段“氛围”改成可见的环境状态；装饰层例外只在成文主规则第 3 条写全。
- 没改的：四段外壳与末行固定句、不写总览句、素材绑定、默认可见运镜、0.5 秒一拍、否定句默认不用、成功项与 asks 保护、轻量默认、两次失败换方法、同音替换、每拍三样。经验库这一轮没写新条目。

## v21 写法分主次（2026-09-23）

- 新增 writing-rules「成文主规则」：每拍只写三样（镜头、画框切线、主事与终态），每镜 1–2 个关键事件放在看得见、有光的位置，装饰层压到全局一两句，发力只写结果，不设字数上限；SKILL.md 必须守住第 8 条指向它。
- 删掉“在最远处才松开”“先转肩再转胯”“借转身惯性”这些例句；L093 的“先收镜再起动作”不再推荐（L099）。
- 段落归属取代 v20 的“镜内写局部材质响应、局部受光”：人物与持物的随动写主体段，天气与环境写场景段，风格段只写不带动作词的材质性格；镜内只写关键事件需要的材质与受光，以及本镜里光的变化。
- “拿不准时保留”收窄到用户要求、认可内容、asks 落点和承重项；改稿新增第 4 类去向。
- check_prompt 新增两类提醒（关键动作紧挨整幅遮挡、发力过程写法）；机制词文案改为“改写看得见的结果”；近景不再算特写；命令区能识别 a-b秒 标题；机制词与发力过程提醒都跳过 --asks 有效要求的关键词；遮挡词前两个字里有否定字就不算；回归 184 → 210。
- 关键事件那一瞬镜头保持持续的跟随或慢推，关键事件之前不停镜；停稳只用在关键事件之后的后果停留或本镜收尾的终点画框（writing-rules 第 29 条），camera.md、camera-terms（M08 与急扫说明）、quality-gate 同步。
- 素材归属补上两处位置：宫格分镜图在情节命令区，首帧、尾帧图在主体段；用户要求全片静音时的“不要任何声音”写主体段，其余必要否定仍写进它管的那一镜。原则不变。
- 经验库新增 L099–L103，L091、L093 加注记，L071 补上规则的新位置，待测清单 T11 标为作废。
- 没改的：四段外壳与末行固定句、素材绑定、默认可见运镜、0.5 秒一拍、否定句默认不用、成功项与 asks 保护、轻量默认、两次失败换方法、同音替换。

## v20 分层整理（2026-09-22）

- SKILL.md 缩为关键底线、任务必读路由、字段深度、轻量/全套触发与交付条件；严格审九步、requirements/review/independent 结构与复核闭环统一归入 `references/review/quality-gate.md`。
- 表头、八列镜头表、代码块与检查行等公开格式统一归入 `references/seedance-format.md`；生成新稿明确不写概述或总览，案例只借镜内组织，案例原文未改。
- 画面质感归属统一：风格段写跨镜共享媒介、材质行为与光影原则；主体/场景写稳定表面与光源；镜内写局部材质响应、局部受光、焦点与曝光变化。保留适用的独立风格控制与原强度。
- 保留默认轻量、L3 不自动严格审、实际正文 check_prompt、父稿/asks/成功项保护、默认可见摄影运动、同音替换、四段末行与既有稿格式继承；未改 hooks、scripts、tests、模型能力、诊断重试策略或任何阈值。

## v19 信息取舍与成功项保护（2026-09-22）

- 写稿按目标观感与必要关系选择信息，关键接触、受力、空间衔接和视觉终点按需展开；取消关键动作数、每拍句数和物件数配额，保留必要质感与可见运动。
- 用户认可的效果、措辞、设计与版本及原话绑定，复用 asks.txt 保护；普通优化、精简、Skill 更新不授权删弱成功项。逐字锁不变，授权范围内等义合并需保留全部独立控制；涉及另一受保护项的取舍先交用户决定。
- 丢句提醒支持按等义落点裁定；长度增长超过 15% 仅作复查提醒，删除“越长必然稀释”和“新增控制必须对应删减”的断言。
- 保留与必要性核对并入现有六域检查，不新增公开概述、逐句证明文件、审核轮次或钩子；不改既有案例和成片经验记录。

## v18 定向修复（2026-09-22）

- 修订时分别保留继承意图与父稿实际格式，四段改稿继续执行四段检查；五、六段及未知旧外壳仍兼容，固定句仍沿用父稿。
- 丢句检查改为同镜、同全局段一对一匹配，先匹配保留句，再匹配近似改写，避免跨镜或重复使用同一句掩盖删除。新增 16 项针对性回归。
- 机制词、程度词可以与可见表现并用；错开摄影与动作、调整亮度峰等未验证修法仅在适用条件下考虑，保留原始观察与来源。
- 清理轻量路径里的全套回执和文件要求、旧总览示例；两条路径各自取真实检查输出，不增加每稿审查步骤。
- 用户明确替换同一字段时，凭新原话更新要求清单，不重复确认；补充要求与未被替换的历史要求继续保留。

## v18（2026-09-22）

- **A 写法规则从 63 条扩到 78 条**：总原则加一句定位（像给盲人做的电影解说词：现在时、只说看得见的、按出现先后说、不解释意图、不评价）；第零组三道门变**五道门**，加「承重性」（这句缺了画面会不会不一样，不会就是装饰、删）与「尺度一致」（点名的细节当前景别装得下吗，装不下等于偷偷改景别）；新增暗处写还留着什么、因果链不是清单、因果用可见表现写不用力学名词、程度词换可数单位与位移证据、相机大动作与主体大动作错开起点、遮挡不跨越位移、物件首次出现要有锚点、距离链要连贯、亮度事件和景别变化错开、效果要有可对比的载体、比喻只用于材质、形容词后跟可观察结果、解释词删；规则 14（原 11）补写作量（全片只有两三个关键动作拆到部位，一拍 2–4 句、每句一个新的可见变化、一拍点名物件不超过 3–4 个）；第八组加「四种失效腔调」小表（小说腔 / 参数腔 / 讲戏腔 / 清单腔）。
- **B SKILL.md 九步同步**：⑦ 成文写清动作拆解与一拍的量、一拍内句子串成因果并用可见表现写、物件首次出现给锚点；⑧ 矛盾核对加「距离链（远处／贴镜不相邻）」，对照口径由三道门改五道门；硬约束第 5 条补一句——机制词（力的传递、惯性、蓄力本身）同样不是画面，写它的可见表现。
- **C `check_prompt.py` 新增五类提醒**（都是警告，词表放脚本头部常量并注「可调」）：`MECHANISM_WORDS` 机制词；`MICRO_SCALE_WORDS` 尺度名词出现在没有特写 / 大特写 / 微距 / 近景字样的镜头正文里时提醒（风格段的材质句不扫）；`ABSOLUTE_VOID_WORDS` 绝对化的空或黑；解释词（仿佛、似乎、像是在、营造、氛围、有一种）并入 `EMPTY_WORDS`，台词不扫；`FAR_WORDS` 与 `NEAR_CONTACT_WORDS` 在同一镜正文里同时出现时提醒距离链。新增七个夹具与 18 项回归（`run_check_tests.py` 166 → 184 项）。
- **D 经验库新增 L087–L097 十一条已试经验**：主体不离画锁、尺度名词隐式改景别、绝对化空黑出死区、物件无锚点即消失、机制词不可拍、否定加明确替代角度有效（补充 L068）、相机大动作与主体大动作抢同一瞬、亮度峰撞景别骤变被当转场、遮挡后位置跳变等于隐形剪辑、效果要有可对比载体、程度词换可数单位与位移证据。均来自同一晚的成片对照，来源写到具体成片文件。

## v17（2026-09-22）

- **A 要求清单**：多轮任务从第二版起在工作目录维护 `asks.txt`（`编号 | 用户提出时间 | 用户原话摘录 | 落点关键词 | 状态`，状态只有"有效"和"撤回（时间＋用户原话）"），每收到一条用户消息先更新清单再改稿，撤回只能由用户原话触发、作者不得自行撤回；`check_prompt.py --asks asks.txt` 对每条"有效"要求逐组核对正文（引号内台词也算正文），没落点或格式不对报错误，summary 写"要求清单 N 条有效全部有落点"，`--report` 里记 `asks_checked`。
- **B 改稿不丢句**：有 `--baseline` 时脚本按句号、分号、问号、感叹号（引号内不切）切句比对，把父稿有、新稿没有的句子列出来提醒（最多 10 句，被改写成相近说法的不算消失，`--partial` 只比对被替换的镜头，已由要求清单报错的那一句不重复报）；新稿去空白字数比父稿多 15% 以上时提醒长度稀释。`revise-rules.md` 新增第 11、12 节：只改引出问题的那一句不重写整段、父稿句子消失只有三种合法理由、变更摘要里要写出消失的句子和归类。
- **C 表头加"保留"行**：多轮任务的交付表头在"锁定 / 待你定 / 升级 / 翻译"之外允许第五行"保留：R1、R2、R4（R3 已按你 03:41 的要求撤回）"，只列编号和撤回说明、不复述原话，单轮任务不写；「禁止事项」新增一条"不在用户没要求的情况下删掉之前设定好的内容；要放弃某条先问"。

## v16（2026-09-22）

- **风格段只描述画面质感**：四方面（画风与渲染方式、材质与表面质感、光的质感与层次、镜头性格＝焦段、景深、手持还是稳定），删掉"表演核心"、"镜头语言"改成"镜头性格"；表演、具体运镜路径、动作与事件、时序一律写进镜内，`check_prompt.py` 在风格段发现时序词或运镜路径与动作词时提醒（两张词表放在脚本常量里，旁注可调）。
- **提示词里不写 @**：素材写 `图N / 视频N / 音频N`（按上传顺序，没有"片"字），粘进即梦以后在软件里再 @ 出来；旧稿修订沿用旧稿写法。脚本把 `@图片N / 图片N / 图N / @视频N / 视频N / @音频N / 音频N` 统一归一成 `图N / 视频N / 音频N`，`--labels` 两种写法都接受；"素材没有用 @ 引用"那条提醒删除，改成四段新稿里出现 @ 时提醒一次（继承模式按父稿）；`hooks/stop_gate.py` 的操作命令识别接受不带 @ 的 `视频N`。
- **每份素材只在用它的那一段绑定一次**：人物与物件图和音频 → 主体段，场景图 → 场景段，材质光影画风参考图 → 风格段，操作类的视频 → 情节段开头的命令句；情节里只用同一个短名指代、不复述职责。同一素材的职责句出现在两个段落时脚本提醒。
- **同一件事只写一次**：跨段完全相同、去掉标点与空白后 ≥12 字的子句（台词不参与）由脚本提醒；SKILL 九步 ⑧ 的重复核对写明跨段重复长句与素材重复绑定由脚本提醒、逐条裁定。

## v15.1（2026-09-22）

- 生成类新稿的情节段直接从镜头标题开始，不写"生成一段 N 秒……一镜到底"这类总览句（时长与镜数由镜头标题表达，"只有一个人物""跨镜保持一致"写进主体段）；check_prompt 发现四段生成稿情节段开头有非命令文字时提醒。

## v15（2026-09-22）

- **A 案例入库规则：可复用点必须落到经验编号**——「可复用点」的每一条要么是知识、带 `→ L0xx`（经验库已有的就引用，没有的当场用 `log_lesson.py` 写一条已试经验，来源写"案例 M00x + 成片文件名"），要么是样板、标 `（样板）`；M001–M004 逐条改造完成，新增 L073–L086 十四条已试经验；新增 `scripts/lint_cases.py` 并接进 `tests/run_check_tests.py`。
- **B 案例条目倒过来写**：顺序改成 标题 → 素材 → 成片文件名 → 我的评价 → 关联经验 → 可复用点 → 提示词原文（`需要抄句式时再读`，折在最后，原文一字未改）；读法改成只读索引选一条，读它的可复用点与关联经验，类型和素材形态都相同又要抄句式时才展开原文；SKILL.md 路由表、九步 ③、读料纪律与 official-cases 措辞同步，lessons/README 加「案例与经验的分工」。
- **C 新增「整理经验」命令**：`scripts/review_lessons.py` 只读输出候选清单（被 2 条以上案例引用、同分类相近可合并、结论里互相修正、分类超 15 条、已升级条目），由用户挑；升级的在原条目结论列加 `【已升级为规则：<文件> <节>】`，合并的后一条加 `【并入 L0xx】`，频率与读法写进 lessons/README。
- **D 两条经验升级成规则**：L071「先并行再删拍」进 SKILL.md 九步 ⑥ 与复杂度分级的动作密度口径；L072「高速连续镜头不回中性状态、转换处继承余势」进 `craft/combat.md` 第 6 节；两条原条目结论列各加一个已升级标记，其它列未动。

## v14.1（2026-09-22）

- 修钩子误拦：Claude Code 的 transcript 里工具返回行和钩子自己的拦截提示都是 role=user，钩子曾拿它们的时间当"本轮用户消息"，把刚生成的报告判成过期，每次交付都拦一次。现在只认用户真正打的消息（含图片消息）。

## v14（2026-09-21）

- **A 经验库前缀改为脚本强制**：`log_lesson.py --topic` 必须是 `分类/主题`（12 个固定分类），不合规退出 1、列出分类、不写入；`merge_lessons.py` 遇到来源条目缺前缀拒绝整次合并；新增 `scripts/lint_lessons.py` 体检全库前缀与 L 编号连续性，`tests/run_check_tests.py` 加一项调用它。文档里"再读最后 10 条兜底"的读法全部删掉，读经验库只 grep 两三个分类。
- **B 全套路径不再自动触发**：只剩"用户说严格审 / 走全套 / 要交付校验"和"事先约定的审核节点"两个条件；"全新 L3 稿的第一版"不再触发，复杂稿默认也走轻量路径，矛盾核对在头脑里做。
- **C 锁定项只认本次对话**：表头"锁定"行只放本次对话里用户明说的；持久记忆、旧项目文件、旧稿、以前会话来的设定写进"待你定"行并注明来源，用户本次确认后才升为锁定；requirements 的 source 同样不接受未经本次确认的记忆与旧文件。
- **D 交付时少说话**：交付只有表头、成品代码块、检查行三样（加一两句例外说明），不写过程叙述、取舍表、改动清单、"这轮做了什么"；改稿的变更摘要 ≤3 行。
- **E 任务没说清之前不预读**：用户还没给出题材、素材、要求之前只载入入口，不读经验库、案例和工艺卡。
- **F 全套路径复核核验范围收紧**：复核者核验修复时只有"修复点没修好""修复引入新矛盾"两类进 unresolved，其它新发现写 independent.json 的 `suggestions` 数组，不进 unresolved、不阻断放行（verify_delivery 只读必填字段，多这个字段不报错，脚本未改）。
- **G 维护方式**：改 skill 在 `~/Documents/Codex/aigc-video-dev`（dev 分支工作区）改，测过再合并到 main 并 `sync.sh`；main 是安装位，改到一半的文件不影响正在使用的会话（写进 README「同步与安装」与 SETUP 第 8 节）。

## v13

- **新稿改成四段外壳**：`主体：` `场景：` `风格：` `情节：`，顺序固定，**不设概述段，也不再有 `结尾：` 标题**。
- **固定句改成提示词的最后一行**，逐字 `全片不添加BGM，不添加字幕。`：必写、只写一次、结束整份提示词，后面不能再有任何内容（检查容忍 BGM 前后的空格与末尾句号有无，写出来一律用规范形式）。
- **其他确有必要的否定句写在固定句上面的几行**，每句一行；正文末尾这几行连续的否定或约束行叫**结尾区**（行首为 `不` / `禁止` / `无` / `全片不添加`，或延长命令的官方约束句），否定预算（自写 4 条含固定句）、`--negative-exception` 逐句核对、延长的官方约束句都按这个区域算。
- **修订继承父稿外壳与父稿那句固定句**：六段旧稿的修订仍是六段、必填词仍在概述段、固定句仍是旧句"不添加字幕，不添加背景音乐。"；五段旧稿的修订仍是五段、固定句仍在结尾段。换句式或换外壳须本次明确授权。
- **操作命令**：必填词仍在情节段开头的命令句；延长的官方约束句改放结尾区、固定句之前；其他操作类原来"写在结尾段"的句子一律改成写在末尾固定句之前。
- `check_prompt.py`：`--format` 取值改为 `四段|五段|六段|继承`，无 `--baseline` 时默认四段；四段稿出现 `结尾：` 或 `概述：` 报错；固定句按模式分别核对（四段用新句且必须独占最后一行，五段 / 六段用旧句在结尾段，继承按父稿那一句）。Stop 钩子代跑时按标题推断外壳：有概述段 → 六段，只有结尾段 → 五段，都没有 → 四段。
- 中途曾把这一版撤回过一次（当时误把固定句改成可选），用户确认固定句必写之后按本节重做。

### v13.1（紧接 v13 的用户决定）

- **末尾只留固定句那一行**：`全片不添加BGM，不添加字幕。` 之前的那一行必须是普通正文（镜头内容或命令句），不能再有否定句、约束句、"保持…"句。**"结尾区"概念取消**（五段 / 六段旧稿的修订仍按旧壳的结尾段规则，不动）。
- **默认不用否定句**：四段新稿自写否定预算 = 0 条（固定句不计）。确属特殊情况、没有正向写法的否定句写在**它管的那一镜的正文里**（或操作命令的命令句里），不放末尾。跨镜的"保持一致"改成正向句写进主体段或情节段开头的总览句。
- **操作命令的官方必填句全部放在情节段开头的命令区**（`情节：` 之后、第一个镜头标题之前）：延长的必填词 + `@视频N` + 官方约束句；编辑的必填词 + `@视频1` + "保持…不变"句 + 唯一母版声明；衔接、首尾帧、白模同理。四段模式下这些句子出现在末尾（固定句之前）会报错。六段旧稿的修订仍在概述段 / 结尾段，五段旧稿的修订仍按父稿。
- `check_prompt.py`：四段模式删掉结尾区提取，改为"末行 = 固定句，倒数第二行不得是收尾行"；否定检查改成**全文逐句提醒**（句首为 `不出现|不添加|不得|不要|不能|不许|不可|禁止|避免`，固定句与引号内台词除外；"没有""无"不抓），`--negative-exception` 逐句点名的不再提醒、点名了却在稿里找不到报错；四段模式 summary 写"镜内否定提醒 N 句"（不写全角括号，钩子的检查行正则按 `）` 截断），五段 / 六段 / 继承仍写"自写否定计数 N 条"并保留结尾段 4 条预算。继承模式的操作必填句命令区与结尾段都接受，按父稿写法。

## v12

- `check_prompt.py` 加 `--report`，轻量路径每次留一份机械检查记录。
- Stop 钩子改成三层判定：有本轮报告对得上就放行；没报告的完整稿钩子代跑检查，有错打回、无错放行并提示"作者未自己跑检查"；局部镜头与操作命令没报告则打回。工具输出围栏里的检查行不核对。
- 仓库化：本机唯一副本 + 两宿主软链，`scripts/sync.sh`、`install.sh`、`SETUP.md`。

## v11

- 新稿五段（主体 / 场景 / 风格 / 情节 / 结尾），不设概述段；修订继承父稿外壳；操作命令必填词写在情节段开头。`check_prompt.py` 加 `--format 五段|六段|继承`。
- 全套路径复核闭环：一次全量独立复核后，作者修复，同一复核者只核验修复点并重绑哈希。
- 两次失败换方法规则加唯一例外：有已试经验记录支持的修法可作第三版。
- 成功反馈自动记录：用户明确评价某版效果好即记一条已试经验。

## v10

- 经验库主题列改为 `分类/主题`（12 个固定分类），读法 = grep 两三个分类 + 最后 10 条，禁止整份读。
- 案例文件只读索引再取 1 到 2 条；入口加"读料纪律"（会话内不重读、不顺着链接读）；放行流程文件只在全套路径读。

## v9

- 流程分级：默认轻量路径（不建需求与审查记录、不派子代理），全套只在全新复杂稿第一版、用户说"严格审"、事先约定审核节点时走，独立复核最多一次。
- 两次失败换方法硬规则（诊断卡第 8 节）。
